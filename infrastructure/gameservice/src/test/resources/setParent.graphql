mutation setParent($parentId:String!, $childId:String!){
    setParent(parentId:$parentId, childId:$childId){
        id
        children{
            id
            parent {
                id
            }
        }
    }
}